You are extracting step-by-step procedures from tutorials for the LangChain Python library. The tutorials describe how to perform various tasks with code and examples that use relevant APIs in the library. Your task is to simplify a tutorial into three parts: a description of the specific goal, the set of necessary resources, and a minimal set of the necessary instructions. Write "Goal: {description}", "Resources: {description}", and ordered numbering for the steps.

The goal should be a complete and specific task rather than a general approach: write "integrate a calculator into a chat model" instead of "integrate custom functions with OpenAI functions agent". Do not refer to LangChain or specific library functionality in the goal description: write "a system that can answer natural language queries about the weather" instead of "use LangChain's APIChain to access a weather API".

The resource line should include any external APIs, LLMs, or hardware necessary to perform the procedure. If a procedure needs an LLM, be sure to include that in the resource line even if the procedure doesn't specify a particular LLM. Don't include code resources, such as LangChain APIs, 3rd party packages, vector stores, etc.

Each instruction must be a logically distinct action, and should explicitly refer to the library methods and objects used for that action. Don't include unnecessary details, code, parameters, or example usage; include only the information strictly necessary to accomplish the procedure.

Do not mention importing the required modules as a separate step. Instead, always use the full path to APIs (e.g. `langchain.chat_models.ChatOpenAI`) when referring to them in the steps. Do not mention specific API parameters unless they're important to understanding how the procedure fits together, e.g. don't include things like the verbosity or the model temperature unless those matter for accomplishing the stated goal.

If the raw text actually contains more than one logically distinct procedure, you may then output multiple sets of goals and instructions for each different procedure (delimited by NEW PROCEDURE), but this should only be done if the goals are fundamentally different. The text of each procedure must be able to stand alone, without relying on details or referring to steps from other procedures.
