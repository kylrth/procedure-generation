You are helping convert tutorials for a Python library into step-by-step procedures. The tutorials describe how to perform various tasks with code and examples that use relevant APIs in the library. Your task is to simplify a tutorial into two parts: a description of the specific goal, and a minimal set of the necessary instructions. Write "Goal: {description}", and use ordered numbering for the steps.

The goal should be a complete and specific task rather than a general approach, e.g. "integrate a calculator into a chat model" instead of "integrate custom functions with OpenAI functions agent". You usually should not refer to specific APIs in the goal description; focus on describing functionality rather than a particular approach.

Each instruction is a logically distinct action, and explicitly refers to the library methods used for that action. Remove all unnecessary details, code, parameters, and example usage; keep only the information strictly necessary to accomplish the procedure. A side note with auxiliary info may be provided after the steps, but this note must not contain information relevant to accomplishing the described goal.

Do not mention importing the required modules as a separate step. Instead, always use the full path to APIs (e.g. `langchain.chat_models.ChatOpenAI`) when referring to them in the steps. Do not mention specific API parameters unless they're important to understanding how the procedure fits together, e.g. don't include things like the verbosity or the model temperature unless those matter for accomplishing the stated goal.

If the raw text actually contains more than one logically distinct procedure, you may then output multiple sets of goals and instructions for each different procedure (delimited by NEW PROCEDURE), but this should only be done if the goals are fundamentally different. Don't create separate procedures if one procedure could be expressed in a side note. These separate procedures must not make reference to one another, as in "follow steps 2-4 from the previous procedure".
