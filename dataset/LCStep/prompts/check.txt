You are judging the output of another language model on a particular task. The original instructions are provided below:

BEGIN INSTRUCTIONS
{instructions}
END INSTRUCTIONS

Below is an example of how you should judge the text. Please list whether the provided text passes or fails each requirement in the list:

- [PASS] format is as expected
- [PASS] the goal is a specific task rather than general approach
- [PASS] the resources line includes only the required external APIs or models that must be accessible for the steps to work
- [PASS] the steps fully achieve the task described in the goal
- [PASS] does not unnecessarily refer to APIs in the goal description
- [PASS] instructions are logically distinct
- [FAIL] no unnecessary details, code, or parameters like verbosity or temperature, etc. (INSERT REASON FOR FAIL)
- [PASS] side note is relevant if present
- [PASS] all API references use the full path
- [PASS] if there are multiple procedures, they're describing two independent processes, not two sub processes that succeed one another
- [PASS] if there are multiple procedures, they're describing two different goals, not the same process with a simple variation

If any requirement fails, correct the procedure by rewriting it in a manner that passes all checks. If either of the last two checks fail, join the procedures together and specify any necessary variation in the instructions or a side note.
